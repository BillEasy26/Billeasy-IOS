#include "App.Shared.xcconfig"

// Em celular físico, localhost não resolve — use sempre a URL de produção.
// Para testar no simulador com frontend local, troque por: http:/$()/localhost:3000
// FRONTEND_BASE_URL = http:/$()/localhost:3000

#include? "Secrets.xcconfig"
